# Development Environment Variables

# Backend API URL
VITE_API_BASE_URL=https://trabien.com

# Desktop deep link callback URL (dev)
VITE_APP_DEEPLINK_SCHEME=deskcal-dev://auth/callback

# Note: Kakao credentials are configured in the BACKEND, not here
# The desktop app only needs to know the backend URL

