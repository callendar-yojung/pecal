# Environment Variables Template

# Backend API URL
VITE_API_BASE_URL=http://localhost:3000

# Desktop deep link callback URL
# - dev 권장: deskcal-dev://auth/callback
# - release 권장: deskcal://auth/callback
VITE_APP_DEEPLINK_SCHEME=deskcal-dev://auth/callback

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Note: With web-based OAuth, the desktop app ONLY needs
# the backend URL. All Kakao credentials are configured
# in the backend server, not in the desktop app.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
